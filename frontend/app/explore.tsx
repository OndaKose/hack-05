// app/explore.tsx
import React from 'react';
import { View, Text } from 'react-native';

// ← default export が必須
export default function ExploreScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Explore コンテンツをここに書く</Text>
    </View>
  );
}